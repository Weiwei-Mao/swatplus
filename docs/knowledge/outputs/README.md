# Outputs

Output controls, aggregation periods, mass-balance summaries, files, columns, and writer routines belong here.

An output note must link a reported column back to its producer and accumulated state. Distinguish a calculation defect from an aggregation or formatting defect, and record whether a value is daily, monthly, yearly, or average annual.

